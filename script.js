// Este archivo es un stub de compatibilidad.
// El código real está en app.js — fuerza recarga si el browser cargó HTML viejo.
(function() {
  if (!document.querySelector('script[src*="app.js"]')) {
    window.location.reload(true);
  }
})();
